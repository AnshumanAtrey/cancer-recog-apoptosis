# Heavy run artifacts only — small docs / JSON results under runs/ are tracked.
runs/**/*.ckpt
runs/**/*.pt
runs/**/*.pth
runs/**/*.safetensors
runs/**/*.pdb
runs/**/cache/
runs/**/wandb/
runs/**/checkpoints/
# EXCEPTION: the KRAS-G12D pMHC structures are small (<120KB) run INPUTS the ODesign Colab notebook
# pulls directly from raw GitHub (no manual upload). Public biology, ATOM records only — track them.
!runs/rung30_kras_g12d/staging/kras_g12d_A1101_free_mut_cropped.pdb
!runs/rung30_kras_g12d/staging/kras_g12d_A1101_free_mut_pmhc.pdb
!runs/rung30_kras_g12d/staging/kras_g12d_A1101_free_wt_pmhc.pdb

# Python
__pycache__/
*.py[cod]
*$py.class
/.venv/
/venv/
/env/
.python-version

# Notebooks
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Heavy model caches (HF, torch hub)
.cache/
hf_cache/

# Step 2 — scRNA-seq data pulls (large; re-fetchable from CELLxGENE Census)
data/cellxgene/
*.h5ad

# RUNG-27a — raw public DBs (VDJdb ~41MB + IEDB ~105MB; re-downloadable). Track the grounding RESULT only.
data/grounding/

# Step 3 — raw GTEx/HPA downloads (large; re-fetchable). Track the audit CSV result only.
data/specificity/*.gct.gz
data/specificity/*.tsv.zip
data/specificity/*.tsv
data/logicgate/

# Local research-memory mirror — PRIVATE (contains unpublished collaborator ideas). Never push public.
memory/

# RUNG-27b — cached driver CDS (re-fetchable from Ensembl MANE)
data/refs/cds/

# Cloned external tools (ProteinMPNN etc.) — not project artifacts
.tools/

# Off-target scan: human transcriptome (large, re-fetchable). Track the RESULT json only.
data/offtarget/

runs/rung31_offtarget/genome_scan.log

# Immunopeptidome atlases (HLA Ligand Atlas etc.; re-fetchable). Track the RESULT json only.
data/immunopeptidome/

# RCSB crystal structure downloads (re-fetchable). Track the staged target only.
data/pdb_kras/

# ODesign CCD assets (408MB cif + 120MB pkl) — too big for git (>100MB limit). Hosted as a GitHub
# Release asset instead; the Colab notebook wgets them. Keep the local copies untracked.
components.v20240608.cif
components.v20240608.cif.rdkit_mol.pkl
