# Heavy run artifacts only — small docs / JSON results under runs/ are tracked.
runs/**/*.ckpt
runs/**/*.pt
runs/**/*.pth
runs/**/*.safetensors
runs/**/*.pdb
runs/**/cache/
runs/**/wandb/
runs/**/checkpoints/

# Python
__pycache__/
*.py[cod]
*$py.class
/.venv/
/venv/
/env/
.python-version

# Notebooks
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp

# Heavy model caches (HF, torch hub)
.cache/
hf_cache/

# Step 2 — scRNA-seq data pulls (large; re-fetchable from CELLxGENE Census)
data/cellxgene/
*.h5ad

# RUNG-27a — raw public DBs (VDJdb ~41MB + IEDB ~105MB; re-downloadable). Track the grounding RESULT only.
data/grounding/

# Step 3 — raw GTEx/HPA downloads (large; re-fetchable). Track the audit CSV result only.
data/specificity/*.gct.gz
data/specificity/*.tsv.zip
data/specificity/*.tsv
data/logicgate/

# Local research-memory mirror — PRIVATE (contains unpublished collaborator ideas). Never push public.
memory/

# RUNG-27b — cached driver CDS (re-fetchable from Ensembl MANE)
data/refs/cds/

# Cloned external tools (ProteinMPNN etc.) — not project artifacts
.tools/

# Off-target scan: human transcriptome (large, re-fetchable). Track the RESULT json only.
data/offtarget/

runs/rung31_offtarget/genome_scan.log
